# Production Health Scorecard

> Last assessed: **2026-05-30 — re-baselined against the 16-agent board review and the open-risk register.** Previous scorecard (9.3 avg) was not defensible against its own remaining-risk list (never deployed, no BAA, no pentest/WAF, app-only multi-tenant isolation, RPO 24h, single-instance everything). The numbers below reflect what an external auditor would score, not what we wish we could ship.
>
> **Overall: 8.1 / 10 · Production Readiness: 6.5 / 10 · Healthcare Readiness: 7.5 / 10 · Enterprise Readiness: 4.5 / 10**

> **2026-06-02 Compliance UIs update**: No dimension score change — this closes a *reachability* gap, not an attestation gap. Three fully-built compliance backends (patient Consent, Break-Glass, Right-to-Erasure) and audit before/after change-history were headless (no `openapi.yaml` paths, no UI); they now have frontends integrated into PatientDetail + Compliance/admin dashboards. Healthcare Compliance stays **6.5** because its dominating blockers (never deployed, no BAA, no HIPAA gap analysis) are unchanged — but two qualitative notes in that row are now outdated: break-glass *approval* (compliance-officer gate) is operator-reachable, and the erasure 3-step + consent grant/revoke workflows are usable in-app rather than API-only. Break-glass remains **self-grant at activation** (detective + approval-gated, not preventive) — that characterization holds. PHI note: change-history redacts the five field-encrypted columns in before/after snapshots, so the feature adds **no** new cleartext-PHI-at-rest surface.

> **2026-06-02 Phase 3 ops update**: Scalability moved 8.0 → 8.5 (PgBouncer connection pooling; `audit_logs` monthly partitioning via migration 0021 unblocks partition-drop HIPAA retention; `audit_partition_months_remaining` gauge + `AuditPartitionLow` alert). Operational Resilience moved 7.5 → 8.0 (quarterly restore drill procedure documented in RUNBOOK §12 with measured RTO tracking; `backup-verify.mjs --restore` now includes audit integrity check; `statement_timeout` + `idle_in_transaction_session_timeout` moved to role-level defaults for pooling-safe enforcement). ADR-009 documents all decisions.

> **2026-05-31 Phase 6 strategic features update**: No scorecard dimension changes — this was a product feature delivery (bilingual clinical content + doctor analytics), not a security or resilience change. Maintainability stays at 9.0 (34 i18n keys added cleanly, no new tech debt). The doctor analytics page adds a new operational dimension not previously scored — relevant to a future "Clinical Effectiveness" dimension if the scorecard expands.

> **2026-05-31 Phase 3 update**: scalability moved 7.5 → 8.0. DB pool tuning + read-path cache + SSE caps landed; PgBouncer and full Redis-Streams SSE migration intentionally deferred.
>
> **2026-05-31 Phase 4 update**: Operational Resilience moved 6.0 → 7.5. Monitoring stack (Prometheus + Alertmanager email + Grafana SSH-tunnel) and automated daily backup container (02:00 UTC, rsync-over-SSH offsite, 30d retention) landed inside `docker-compose.prod.yml`. RUNBOOK §10–§11 (rollback procedure with image-tag pinning, per-alert playbooks, manual-backup recipe) closed the "nothing has run against a real failure" footnote to "nothing has been *drilled* against a real failure" — the controls are now wired, not just rule files. Production Readiness moved 6.5 → 7.0 (deploy/rollback is now a documented two-step env flip, not an undocumented redeploy). Still open until first deploy + chaos drill: warm standby (D2), PITR/WAL streaming, edge WAF.

| Dimension | Score | Verdict |
|---|---|---|
| **Architectural Quality** | **9.5**/10 | Strong patterns: monorepo + OpenAPI contract + Orval codegen, single-function `evaluate()` kernel, route-access contract test pinning frontend nav ↔ backend RBAC, ESLint-enforced service/route boundary. Closed (2026-05-31): Postgres Row-Level Security (RLS) code-rolled out across all clinical and operational backend services via `runInTenantContext()`. Closed (2026-06-02, F-01 + F-07): RLS now actually enforces in production — migration 0020 creates `medicore_app` (NOSUPERUSER NOBYPASSRLS); api/worker connect as that role; ESLint blocks bare `db` in services/**; break-glass wrapped in runInTenantContext; integration-db test harness connects as medicore_app. |
| **Production Readiness** | **7.0**/10 | Code is production-quality and CI is green; the platform has never been deployed. No BAA, no HIPAA gap analysis. Closed (2026-05-31 Phase 4): api/worker `image: ${API_IMAGE:-…}` with build fallback enables tag-pinned redeploys + 2-step rollback; CI emits the deploy tag (short SHA) to the GitHub Actions step summary; RUNBOOK §10 documents env-snapshot → flip → `up -d --no-build` with migration-direction check (Drizzle has no down migrations). Extracted background worker hardened (read-only rootfs, dropped caps, no-new-privileges). No commercial edge WAF (Cloudflare/AWS WAF) yet. |
| **Scalability** | **8.5**/10 | Designed for horizontal scale (Redis pub/sub EventBus, pluggable stores, cursor pagination, doctor-scope cache). Closed (2026-06-02 Phase 3 ops): PgBouncer transaction-pooling service in compose — api/worker route through pgbouncer:6432; 20 real connections to Postgres serve up to 200 client connections; `statement_timeout` + `idle_in_transaction_session_timeout` moved to `ALTER ROLE medicore_app SET ...` for pooling-safe enforcement; pool math in RUNBOOK §11.6; unblocks 2nd API replica via config change only. `audit_logs` monthly partitioned (migration 0021, ADR-009): composite PK (id, created_at), 132 monthly partitions 2026–2036 + DEFAULT, HIPAA 7-yr partition-drop retention now O(1) DDL, `AuditPartitionLow` alert + gauge. Closed (2026-05-31 Phase 3): DB pool tuned; `CacheService`; SSE caps. Serial integer PKs on legacy PHI tables remain an enumeration surface. SSE Redis Streams deferred — trigger is patient portal traffic. |
| **Maintainability** | **9.2**/10 | The strongest dimension. Single JWT_SECRET source, co-located TTL constants, Phase 2 kill-switches centralized, route-access contract test catches RBAC drift, drizzle-zod guards every JSONB column, CI `validate:errors` blocks orphan codes, exhaustively documented invariants. Closed (2026-06-02, F-03): 25 frontend tests now block CI — route-access matrix (19 tests) pins every role×route combination, i18n parity guard catches silent AR fallback, Guard render smoke proves AccessDenied panel and children render correctly in EN+AR. The documentation-as-code discipline is better than most funded teams. |
| **Operational Resilience** | **8.0**/10 | Closed (2026-06-02 Phase 3 ops): Quarterly restore drill procedure documented in RUNBOOK §12 with measured RTO tracking, ephemeral DB provisioning, and post-restore verification (patient row-count + erasure-blackout + audit integrity check). `backup-verify.mjs --restore` now includes `checkAuditIntegrity()` — fails the drill if `audit_integrity_checks` has any `status='mismatch'` rows in the restored DB. `statement_timeout` + `idle_in_transaction_session_timeout` moved to role-level defaults (migration 0021) for correct behavior under PgBouncer `DISCARD ALL`. Closed (2026-05-31 Phase 4): Prometheus + Alertmanager + Grafana live containers; daily backup container; RUNBOOK §10–§11. Open: Postgres single point of failure with RPO ≈ 24h — moving above 8.5 requires landing D2 (warm standby) + D3 (WAL streaming) and completing the first live chaos drill with measured RTO. |
| **Security Posture** | **8.0**/10 | Crypto and design are modern and largely correct: EdDSA + JWKS + kid rotation, jti replay defense, fail-closed revocation, CSRF as Origin-exact + double-submit inside the kernel, helmet CSP without `unsafe-inline` on style-src, SameSite=Strict HttpOnly cookies, login-shield + DB rate limiter + global mutation limiter, secrets file-mounted (no `docker inspect` leak). Closed (2026-06-02, F-01): RLS now genuinely enforces in production — api/worker connect as `medicore_app` (NOSUPERUSER NOBYPASSRLS), not the superuser; ESLint guard prevents regression; break-glass wrapped; integration tests prove non-superuser connection. Open: never penetration-tested, no DAST, no edge WAF, single field-encryption DEK on the app host (no KMS / envelope-with-master-key), 64-bit `fph` fingerprint is close to security theater with real UX cost. The problem is attestation, not design. |
| **Healthcare Compliance** | **6.5**/10 | Controls designed, not attested. Patient-consent gating before record/Rx creation, three-step right-to-erasure with backup-blackout coordination, PHI field encryption + log redaction, no PHI in SSE payloads, 7-year retention via soft-delete. Open: no formal HIPAA gap analysis, no signed BAA, never deployed to a HIPAA-eligible host, regulatory regime ambiguous (HIPAA + GDPR cited; bilingual EN/AR implies MENA — no data-residency model). Break-glass is self-grant (detective, not preventive — Phase 0 narrowed the role gate; full preventive workflow still open). Audit hash-chain is self-computed (no WORM / offline signing key). |
| **Technical Debt** | **8.5**/10 | Low and well-tracked. Versioned Drizzle migrations, legacy `requireAuth`/`requireRole` shims in 19 route files explicitly scheduled for kill-switch + cleanup, Phase 2 kill-switch scaffolding (`isPhase2Enabled()`) documented with deletion date, `invoices.items` JSONB dropped in favor of normalized `invoice_items`, UUIDv7 PK pattern established with serial-table migration schedule. Open: pervasive `(req as any).user` / `req.id` casts; `strictFunctionTypes` and `noUnusedLocals` intentionally OFF — tightening would catch the exact `req.user` foot-guns from Phase 0. |

## Open CRITICAL / HIGH risk register

Sourced from the 16-agent board review (2026-05-30). This list is the truth of remaining risk — the dimension scores above are derived from it, not the other way around. **2026-05-31 update**: Phase 0/1/2/3 of the roadmap landed (commit set tagged in CHANGELOG.md); items below reflect the post-landing state.

- **[CRITICAL]** Never deployed / no BAA / no HIPAA gap analysis. Cannot legally process real PHI; all prod claims unproven.
- **[CRITICAL]** ~~Multi-tenant isolation is app-layer only~~ **closed (2026-06-02, F-01).** DB-layer backstop is now real in production: migration 0014 adds `CHECK (clinic_id > 0)`; 0015 enables RLS; 0017 adds doctor-scope RESTRICTIVE policy; 0020 creates `medicore_app` (NOSUPERUSER NOBYPASSRLS). api/worker connect as medicore_app so RLS actually enforces — not nominal. ESLint guard prevents regression. Integration-db tests prove the isolation in a non-superuser context.
- **[HIGH]** Postgres SPOF, RPO ≈ 24h, RTO 4h SEV-1, drills only theoretical.
- **[HIGH]** No penetration test, no edge WAF, **DAST now partly closed (2026-05-31)** via OWASP ZAP baseline workflow at `.github/workflows/security-dast.yml` (runs nightly against `STAGING_URL` once provisioned). Caddyfile gained `@scan_paths` and `@bad_method` matchers — basic edge WAF rules block common probes / disallowed HTTP methods. No commercial-grade WAF (Cloudflare / AWS WAF) yet.
- **[HIGH]** Scope-guard foot-gun **closed (2026-05-30)** — `assertPatientInScope` / `assertMedicalRecordInScope` throw `ForbiddenError`, callers updated, scope unit tests pinned.
- **[HIGH]** PHI isolation guarantee **moved off mocks (2026-05-30/31).** Real-Postgres integration suite via testcontainers (`tests/*.integration-db.test.ts`) now covers cross-tenant isolation, doctor-scope, RLS dormant-and-active states, `clinic_id` CHECK constraint enforcement, and doctor-scope RLS WITH CHECK rejection of out-of-scope writes. The existing mocked suite remains for unit speed.
- **[HIGH]** Enterprise gaps: no SSO / SCIM, governance single-author. Unchanged.
- **[HIGH]** ~~Pervasive `(req as any).user` casts~~ **closed (2026-05-31).** `src/types/express-augment.ts` declares `Request.user` and `Request.id` on the global Express namespace; all `(req as any).user` / `(req as any).id` casts removed.
- **[HIGH]** ~~Audit silently drops events on missing `req.user`~~ **closed (2026-05-31).** `logAudit()` now routes user-less events to `SYSTEM_USER_ID = -1` and increments `audit_system_actor_total{action,entity_type}` for observability — events are never lost.
- **[MEDIUM]** Single field-encryption DEK on the app host (no KMS / envelope). **Scaffolding landed (2026-05-31)** — `KeyProvider` interface + `LocalEnvKeyProvider` impl in `lib/key-provider.ts`; cloud-KMS swap is documented as a one-line change when an actual KMS is provisioned.
- **[MEDIUM]** ~~Break-glass is self-grant~~ **downgraded (2026-05-31).** Now activate-with-grace + compliance-approval workflow: 5-minute grace window from activate, full 15-minute TTL only after `compliance_officer` calls `POST /break-glass/sessions/:id/approve`. Self-approval forbidden. Compliance revoke of unapproved sessions logs `BREAK_GLASS_DENIED`. The remaining preventive gap: an attacker with 5 minutes can still read PHI before compliance reacts — true synchronous approval is incompatible with real emergencies.
- **[MEDIUM]** 64-bit `fph` fingerprint with real UX cost. Unchanged.
- **[MEDIUM]** Audit hash-chain self-computed (no WORM / offline signing). Deferred — depends on KMS.
- **[MEDIUM]** No load testing / N+1 audit. Unchanged.
- ~~**[MEDIUM]** `audit_logs` unpartitioned~~ **closed (2026-06-02, migration 0021).** Monthly RANGE partitions 2026-01→2036-12 + DEFAULT. Partition-drop for HIPAA 7-yr retention is now O(1) DDL. `AuditPartitionLow` alert fires when headroom drops below 24 months.

The full remediation roadmap is at `~/.claude/plans/agent-1-chief-cryptic-pancake.md`.

## What moved the needle

- 🔴→🟢 **Auth**: Hand-rolled crypto → `jose` HS256 + `jti` revocation
- 🔴→🟢 **Monitoring**: Zero → Prometheus/Grafana + alert rules
- 🔴→🟢 **Backups**: None → `pg_dump` script + weekly verification
- 🔴→🟢 **Audit**: Partial → 100% PHI coverage (`logRead`/`logAudit`/`logDenied`); `admin` removed from audit access
- 🔴→🟢 **CSP**: API-only → real SPA coverage via Vite plugin + `<meta>` tag; Vitest regression guard
- 🔴→🟢 **ESM runtime**: `require()` → `await import()` + top-level await in `runtime/index.ts`
- 🔴→🟢 **Revocation fail-closed**: `verifyToken` throws on store error (was `console.warn` + allow)
- 🟡→🟢 **Testing**: No tests → Vitest unit + Supertest integration suite
- 🟡→🟢 **Auth kernel (v7)**: Single `evaluate(req, scope, allowedRoles?)` kernel — CSRF, identity, revocation, jti, fingerprint, role. Returns a `Decision` discriminated union. 25-test unit suite covers every branch.
- 🟡→🟢 **JWT TTL**: Flat 8h → per-role (super_admin 15m, admin 1h, clinical 2h, operational 4h). Cookie `maxAge` aligned to per-role TTL.
- 🟡→🟢 **Billing SoD**: front_desk creates; `billing_manager` pays/cancels; cancel requires reason ≥ 30 chars; anti-fraud gate blocks same-user pay within 30s of create
- 🟡→🟢 **Privileged scope**: `PATCH/DELETE /users/:id`, `POST /users/:id/reset-password` use `authGate("privileged")` — jti consumed on use
- 🟡→🟢 **TTL constants**: `MAX_ROLE_TTL_SEC` in `lib/auth-constants.ts` (re-exported from `lib/auth.ts`), imported by revocation stores to avoid ESM circular dependencies
- 🟡→🟢 **JWT_SECRET single source**: `lib/jwt-secret.ts` (2026-05-24) — `auth.ts` + `policy.ts` both import from it; duplicate declaration eliminated
- 🟡→🟢 **ROLE_TTL + COOKIE_TTL_MS co-located**: both in `auth-constants.ts` (2026-05-24) — drift between JWT expiry and cookie maxAge structurally impossible
- 🟡→🟢 **Route-level code splitting**: all 22 pages `React.lazy()` + `Suspense` (2026-05-24)
- 🟡→🟢 **Root ErrorBoundary**: rendering crash in any page no longer kills the app (2026-05-24)
- 🟡→🟢 **`/metrics` gated**: `METRICS_TOKEN` bearer token in prod (2026-05-24)
- 🟡→🟢 **Login shield**: `middlewares/login-shield.ts` — 4 KB body gate, Content-Type enforcement, empty UA rejection in prod, 20 req/15 min IP layer
- 🟡→🟢 **CI pipeline**: `.github/workflows/ci.yml` — typecheck, validate:errors, api-server tests
- 🟡→🟢 **Compliance Dashboard**: `GET /dashboard/compliance` + `ComplianceDashboard.tsx` — audit event counts, 7-day trend, top entities/users, denied events feed
- 🟡→🟢 **Per-role UI/UX redesign (2026-05-18)**: All 10 roles now land on a dedicated workspace. 7 new dashboard endpoints + pages (Billing, Pharmacist, Nurse, FrontDesk, Imaging×2, Compliance). `getLandingRoute(role)` in `route-access.ts` drives post-login redirect. `navPinnedByRole` elevates each role's primary section in the sidebar. `RoleQuickActions` and `RoleContextLine` in the 40px top bar. `EmptyState` component. `DataTable` density prop. Status palette deduplicated + differentiated (scheduled→slate, requested→sky, in_consultation→violet, paid→emerald). Full EN+AR i18n for all new strings.
- 🟢→🔴 **MFA removed**: TOTP, recovery codes, `mfa_sessions`, two-step gate — fully removed. Single-step login for all roles.
- 🟡→🟢 **Cron state machine (2026-05-25)**: No-show cron routes through `validateTransition("no_show", "scheduled", "system")`; bulk update uses `check.toStatus` not a hardcoded string.
- 🟡→🟢 **VACUUM removed from app layer (2026-05-25)**: Cron block deleted; PostgreSQL autovacuum handles table maintenance.
- 🟡→🟢 **Audit before/after state (2026-05-25)**: `before_state`/`after_state` JSONB columns on `audit_logs`; `logAudit` signature updated; UPDATE paths pass states separately.
- 🟡→🟢 **Invoice items normalization (2026-05-25)**: `services_catalog` + `invoice_items` tables created; `createInvoice` dual-writes to normalized table.

- 🟡→🟢 **Express trust proxy (2026-05-27)**: `app.set("trust proxy", "loopback, linklocal, uniquelocal")` added before all middleware. Behind Caddy on the docker bridge network `req.ip` previously resolved to the upstream container IP — rate-limit keys and audit IP fields all collapsed to one identity. 5 regression tests in `trust-proxy.test.ts` pin the setting.
- 🟡→🟢 **Audit logs compound index (2026-05-27)**: `audit_entity_time_idx (entity_type, entity_id, created_at)` added to `audit_logs`. Enables efficient HIPAA compliance queries. Migration `0004_sudden_spectrum.sql` generated.
- 🟡→🟢 **Cursor pagination fully wired end-to-end (2026-05-27)**: All 12 frontend call sites (11 pages + `CommandPalette.tsx`) migrated from `offset: 0` to cursor-based parameters. `Appointments.tsx` and `DoctorDashboard.tsx` `.data` unwrap fixes a latent runtime bug where `.map()` was called on a `{ data, nextCursor }` object. OpenAPI spec updated; generated clients regenerated. CI guard blocks regression.
- 🟡→🟢 **PHI-decryption-capable secrets file-mounted (2026-05-27)**: `SESSION_SECRET`, `FIELD_ENCRYPTION_KEY`, `METRICS_TOKEN` removed from `environment:` block in `docker-compose.prod.yml` — now read from `/run/secrets/*` by the entrypoint. `docker inspect medicore-api` no longer shows these values. Entrypoint fails closed if any secret file is missing or empty.
- 🟡→🟢 **Route-access contract test (2026-05-27)**: `route-access.contract.test.ts` (67 tests) pins invariant that every frontend-allowed role can reach its page's primary backend endpoint without 403. Found and fixed active production bug: `pharmacist` role missing from `GET /prescriptions` and `GET /prescriptions/:id` — pharmacists got 403 on their own landing page.
- 🟡→🟢 **CI security tooling (2026-05-27)**: CodeQL SAST (`codeql.yml`), Trivy fs scan (`security-scan.yml`, HIGH/CRITICAL, `ignore-unfixed`, SARIF), and CycloneDX SBOM (`sbom.yml`, Anchore Syft) all added as GitHub Actions workflows. Closes the SAST / dep-CVE / SBOM gap.
- 🟡→🟢 **Field-encryption KID envelope (2026-05-27)**: `lib/field-encryption.ts` rewritten with KID-aware envelope `enc:v2:<kid>:<iv>:<tag>:<data>`. Key registry supports `FIELD_ENCRYPTION_KEY` (kid=1) + `FIELD_ENCRYPTION_KEY_NEXT` (kid=2). `FIELD_ENCRYPTION_KEY_WRITE_KID` controls active write key. Legacy `enc:v1:` envelopes still decrypt via kid=1. Rotation now possible without stop-the-world re-encryption sweep. 19-test suite pins all paths. `SECURITY.md` has six-step rotation procedure.

- 🟡→🟢 **Erasure ↔ backup blackout coordination (2026-05-28)**: `erasure_blackout_until timestamp` column added to `erasure_requests`. `executeErasure()` sets it to `executedAt + BACKUP_RETENTION_DAYS` (default 7 days). `backup-verify.mjs --restore` now queries for active blackouts after the restore drill and fails if any are found, preventing silent PHI resurrection. RUNBOOK §2.2 has the manual re-anonymization SQL for emergency restores. Migration `0007_wide_zarda.sql`.

- 🟡→🟢 **Audit transactional outbox (2026-05-28)**: `audit_outbox` queue table (unindexed, no FK constraints) added. `logAudit()` now writes to the outbox; a 5-second drain worker (`startAuditDrain()` in `cron.ts`) transfers rows to `audit_logs` with exponential backoff (5s / 30s / 2min / 10min) and up to 5 retries. Rows exhausted after 5 attempts are counted in `audit_log_write_failures_total` + logged `audit_outbox_row_exhausted`. Final drain flush on graceful shutdown before `pool.end()`. `auditOutboxDepthGauge` Prometheus gauge added. The MEDIUM "audit fire-and-forget" risk row is closed.

- 🟡→🟢 **`invoices.items` JSONB dual-write dropped (2026-05-28)**: `ALTER TABLE invoices DROP COLUMN items` (migration `0005_charming_psynapse.sql`). `invoice_items` normalized table is now the sole source of truth. All billing service functions source line-items via two helpers: `fetchInvoiceItems()` (single invoice) and `fetchInvoiceItemsBatch()` (list). API response shape unchanged. Eliminates the dual-source-of-truth desync risk.

- 🟡→🟢 **Phase 2 OpenAPI sync (2026-05-28)**: All 8 Phase 2 routes declared in `lib/api-spec/openapi.yaml`; Orval codegen re-run. Fixed csp-report path bug (`/api/csp-report` → `/csp-report` relative to apiRouter). Moved Phase 2 routers before domain routers with global `router.use(requireAuth)` catch-all — anonymous Phase 2 endpoints (forgot-password, reset-password, verify-device, wasnt-me, csp-report) no longer intercepted by the auth gate. 24-test `phase2.integration.test.ts` suite covers enumeration prevention, token validation, privilege gating, flag-off 503 behavior, and UUID validation. 398/398 tests passing.

- 🟡→🟢 **clinic_id service-layer enforcement (2026-05-28)**: `clinic_id integer NOT NULL DEFAULT 1` added to `usersTable`, `operationsTable`, `inventoryTable` (migration `0008_acoustic_cassandra_nova.sql`). `TokenPayload.clinicId` (optional, backward-compat) + `AuthUser.clinicId` (required, kernel fills `?? 1`). `loginUser()` embeds clinicId in the JWT. Every PHI service query (10 service files: patients, appointments, medical-records, prescriptions, lab, xray, ultrasound, billing, operations, inventory) now conditions on `eq(table.clinicId, req.user!.clinicId)`. Cross-tenant reads structurally blocked at the service layer. The CRITICAL "multi-tenancy decorative" risk row is downgraded to MEDIUM (Postgres RLS + per-tenant DEK remain open).

- 🟡→🟢 **SSE graceful drain (2026-05-28)**: `GET /notifications/stream` returns 503 + `Retry-After: 10` when `isShuttingDown()` is true. `closeAllSSEClients()` now sends `retry: <jitter>` + `event: reconnect` per connection (jitter 5 000–15 000 ms, randomized per connection) instead of the old `event: shutdown`. Frontend `use-notifications-stream.ts` handles the `reconnect` event and uses the server-supplied `retryAfter` delay so clients spread their reconnects across the jitter window. Shutdown sequence gains `SSE_DRAIN_MS` (default 10s) hold step: new SSE connections blocked immediately on SIGTERM, existing connections kept alive for 10s before the reconnect event is sent. `SHUTDOWN_TIMEOUT_MS` raised from 25s to 30s; `stop_grace_period: 35s` added to both compose files so Docker never SIGKILL during the sequence. 10 new tests in `sse.test.ts`.

- 🟡→🟢 **P3-2 — Service worker (2026-05-29)**: `artifacts/clinic/public/sw.js` added. Network-only for `/api/*` (PHI never in CacheStorage). Network-first + stale fallback for navigation. Cache-first for hashed static assets (script/style/font/image). `import.meta.env.PROD`-gated registration in `main.tsx` (Vite HMR unaffected). `nginx.conf` adds exact-match `location = /sw.js { Cache-Control: no-store }` before the generic JS rule to ensure SW updates deploy immediately.
- 🟡→🟢 **P3-1 — `cookie-signature` dedup (2026-05-29)**: `pnpm.overrides` in root `package.json` forces `cookie-signature@^1.2.2` for all packages. `cookie-parser@1.4.7` (previously pinned to `1.0.6`) now resolves to `1.2.2`. One version in the lockfile.

- 🟡→🟢 **P2-6 — `medical_records.isGlobal` modeling smell resolved (2026-05-29)**: `is_global` and `global_reason` columns dropped from `medical_records`. New `clinic_notices` table (`id`, `clinicId`, `title`, `content`, `createdBy`, `reason`, soft-delete). Migration `0011_clinic_notices.sql` data-migrates any existing `is_global=true` records before dropping the columns. `assertMedicalRecordInScope` in `scope.ts` simplified to a single rule (doctorId ownership only — no global bypass). `PATCH /medical-records/:id/global-flag` route removed; new `GET/POST/DELETE /clinic-notices` routes added (all-roles read; super_admin write). 9-test `clinic-notices.service.test.ts` suite covers CRUD + validation.

- 🟡→🟢 **P2-2 — `style-src 'unsafe-inline'` removed from CSP (2026-05-29)**: `'unsafe-inline'` dropped from `style-src` in both the Helmet header and the Vite prod meta-tag. Root cause: `ChartStyle` in `chart.tsx` was the only real `<style>` injector, but `ChartContainer` is never imported in any production page. React `style={}` props use DOM API — not governed by `style-src`. `ChartStyle` replaced with a no-op; CSS custom properties now set via `useMemo` on the container div. CSP regression guard added to `csp.test.ts`.

- 🟡→🟢 **P2-4 — OpenTelemetry distributed tracing (2026-05-29)**: `lib/tracer.ts` added — `initTracer()` (OTLP SDK, BatchSpanProcessor), `withSpan<T>()` (context-propagating span helper, PHI-safe), `httpSpanMiddleware` (HTTP server spans on every request). No-op when `OTEL_EXPORTER_OTLP_ENDPOINT` unset (zero overhead). `@opentelemetry/*` packages bundled into `dist/index.mjs` (not externalized) so the minimal Docker runtime image needs no extra node_modules. 9-test suite covers all public exports. Typecheck fixed: `lib/db` declarations rebuilt, `phase2.flagON` mock extended with new `usersTable` columns + `role as const` narrowing.

- 🟡→🟢 **Flow 2 — Materialized `doctor_patients` scope table (2026-05-29)**: `getDoctorPatientScope` migrated from `db.selectDistinct` on `appointmentsTable` (O(N) full-scan per cache miss) to `db.select` on `doctorPatientsTable` (O(1) indexed lookup). New `doctor_patients` table with composite PK `(doctorId, patientId)`, `dp_doctor_idx` + `dp_clinic_idx`. Migration `0009_smooth_hellfire_club.sql` backfills from existing appointments (idempotent). New `recordDoctorPatientLink()` export in `scope.ts` (idempotent upsert, `ON CONFLICT DO UPDATE`). Called from `createAppointment` and `patchAppointment` (not `cancelAppointment` — historical scope preserved). Redis cache layer remains but is now optional. Scalability score updated.

- 🟡→🟢 **P2-3 — Additional Prometheus alerts (2026-05-29)**: `prometheus-alerts.yml` expanded from 4 to 11 rules across 2 groups. 4 new API-level rules (no extra exporters): `AuditLogPermanentLoss` (CRITICAL, `for: 0m` — fires immediately on any exhausted audit outbox row; HIPAA §164.312(b) incident class), `AuditOutboxBacklog` (WARNING, sustained outbox > 100 rows for 10 min), `HighNodeMemory` (WARNING, RSS > 768 MB for 5 min), `HighEventLoopLag` (WARNING, P99 lag > 500ms for 2 min). 3 infrastructure-level rules pre-wired for when external exporters are deployed: `RedisHighMemory` (redis_exporter), `DiskSpaceCritical` (node_exporter), `SSLCertificateExpiringSoon` (blackbox_exporter). 9-test `metrics.test.ts` suite pins the `/metrics` bearer-token gate and verifies all custom metric names are registered.

- 🟡→🟢 **EdDSA + JWKS migration (2026-05-28)**: JWT signing migrated from HS256 symmetric (`SESSION_SECRET`) to Ed25519 asymmetric. `lib/jwt-secret.ts` rewritten: `signingKey` (Ed25519 private KeyObject), `jwksDocument` (public JWKS, no `d` component), `jwksVerify` (GetKeyFunction). `/.well-known/jwks.json` endpoint live. Key rotation overlap via `JWT_PREV_PUBLIC_KEY` — old tokens verify while new writes use new key. `SESSION_SECRET` retained as HMAC key for device fingerprinting only. `lib/auth.ts` + `lib/policy.ts` both updated to EdDSA. Docker secrets updated (`jwt_private_key` + `jwt_public_key`). 9-test suite covers JWKS structure, round-trip, unknown kid rejection, backward compat, rotation overlap. **Breaking change**: all existing HS256 sessions invalidated on deploy (expected, one-time).

- 🟡→🟢 **Dockerfile + docker-compose (2026-05-25)**: Multi-stage api build (esbuild bundle + Node 24 Alpine runtime). Clinic SPA built with Vite, served via nginx with SPA fallback + /api proxy. `migrate` init container applies Drizzle migrations before API boots.
- 🟡→🟢 **Versioned migrations (2026-05-25)**: `drizzle-kit generate` + `drizzle-kit migrate` replace push-only workflow. Initial migration `0000_rare_silver_fox.sql` committed (18 tables). CI `migration-drift` job blocks schema drift.
- 🟡→🟢 **ALLOWED_ORIGINS CORS (2026-05-25)**: `app.ts` reads `ALLOWED_ORIGINS` env var in addition to `REPLIT_DOMAINS`, enabling deployment to any host without Replit-specific config.
- 🟡→🟢 **PHI field-level encryption (2026-05-25)**: AES-256-GCM via `lib/field-encryption.ts`. Encrypts `diagnosis`, `vitals`, `medications`, `allergies`, `emergencyContact` on write; decrypts on read. `FIELD_ENCRYPTION_KEY` required in prod; dev bypass with warning. Backward-compatible (unencrypted legacy values pass through).
- 🟡→🟢 **Cursor pagination (2026-05-25)**: All 7 list endpoints (`patients`, `appointments`, `lab`, `xray`, `ultrasound`, `prescriptions`, `medical-records`) — `?offset` → `?cursor=<id>`, `ORDER BY id DESC`, hard max 100. Response: `{ data, nextCursor }`. Eliminates offset drift on 100k+ tables.
- 🟡→🟢 **Redis doctor-scope cache (2026-05-25)**: `getDoctorPatientScope` caches `doctor_scope:<id>` at 60s TTL via optional `runtime.scopeCache`. Per-request full table scan eliminated.
- 🟡→🟢 **Phase 2 auth hardening shipped flag-OFF (2026-05-26)**: 4 new tables (`user_devices`, `device_verification_tokens`, `password_reset_tokens`, `csp_reports`), 7 new routes, 2 new middlewares (`denyIfDeviceUnverified`, `requireStepUp`), 5 new services (email/Resend, sms/Twilio stub, device-trust, device-verification, password-reset, csp-report), 3 new frontend pages (`ForgotPassword`, `VerifyDevice`, `AccountDevices`). `useSessionTimeout` now consumes JWT `exp` instead of hardcoded 30 min. Migration `0002_harsh_monster_badoon.sql` generated. 278/278 tests pass with flag off — code is dormant until `PHASE2_DEVICE_TRUST_ENABLED=true`.
- 🟡→🟢 **Patient consent framework (2026-05-25)**: `patient_consents` table + service + routes. `treatment` consent required before `createMedicalRecord` or `createPrescription` — enforced at service layer.
- 🟡→🟢 **Break-glass emergency access (2026-05-25)**: `break_glass_sessions` table. Any user can activate a 15-minute emergency session for a patient. Immediate SSE alert to all `compliance_officer` users. Every PHI access during the session logged with `BREAK_GLASS_ACCESS` action.
- 🟡→🟢 **Right-to-erasure workflow (2026-05-25)**: `erasure_requests` table. Three-step process: request → approve (compliance_officer) → execute (super_admin). Execution anonymizes all PHI atomically in a transaction. Irreversible per ADR-005.
- 🟡→🟢 **Data retention cron (2026-05-25)**: Monthly (1st @ 03:00). Reports overdue audit logs (>7yr), open erasure requests. Alerts compliance_officers via SSE.

## Remaining risks

Only open items appear here. For resolved items, see "What moved the needle" above or `CHANGELOG.md`.

| Risk | Severity | Status |
|---|---|---|
| Cloud host cutover (AWS/GCP/Azure provisioning, BAA signing, DNS) | CRITICAL | Open — infrastructure code ready; ops provisioning + BAA signing pending |
| Phase 2 device-trust + email-verify activation in prod | HIGH | Code-landed flag-OFF 2026-05-26; activation pending OpenAPI sync, Drizzle migration apply, flag-flip rehearsal (see ROADMAP Phase 2) |
| No penetration test | HIGH | Open — schedule before launch |
| No WAF | MEDIUM | Login-shield + trust proxy provide in-app defense; CDN/edge WAF (Cloudflare or equivalent) still open |
| Multi-tenancy (Postgres RLS) | Closed | Row-level security is fully active across all service paths via `runInTenantContext()`, providing database-enforced multi-tenant isolation alongside the service-layer filters. |
| JWT key pair not yet in secrets dir | HIGH | **Deploy action required**: create `./secrets/jwt_private_key` + `./secrets/jwt_public_key` (mode 0600) before next `docker compose -f docker-compose.prod.yml up -d`. Atomic generator in `secrets/README.md`. |
| Serial integer PKs | LOW | Enumeration surface; sharding blocker. Phase 6. |
| Legacy HMAC hashes | LOW | `password.ts` auto-migrates on next login |
| No formal HIPAA gap analysis | HIGH if US | Open — Phase 5 ops/legal work |
| `REDIS_PASSWORD` still env-passed | LOW | Compose interpolates it into `REDIS_URL` at parse time; migrating to file-mount requires an entrypoint rewrite. Lower priority than PHI-decryption-capable secrets (now resolved). |
| Postgres SPOF / no PITR | HIGH | Single container, `pg_dump` nightly. RPO ≥ 24h. Move to managed PG with sync replica + 5-min PITR before BAA signing. |
| Docker image digests not pinned | LOW | `postgres:16-alpine` and `redis:7-alpine` pinned by tag only. Blocked locally (no docker CLI). Run on deploy host per `docker-compose.prod.yml` comment. |
